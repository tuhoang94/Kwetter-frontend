import { SearchKweetPipe } from './search-kweet.pipe';

describe('SearchKweetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchKweetPipe();
    expect(pipe).toBeTruthy();
  });
});
